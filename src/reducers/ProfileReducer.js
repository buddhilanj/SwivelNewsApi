const initialState = {
  username:'Test',
  custom: null,
};

const ProfileReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default ProfileReducer;
