const initialState = {
  headlines: [],
  customnews: [],
};

const NewsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default NewsReducer;
